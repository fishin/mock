{
  "name": "mock",
  "version": "0.0.3",
  "description": "mock ======",
  "main": "lib/index.js",
  "repository": {
    "type": "git",
    "url": "git://github.com/fishin/mock"
  },
  "author": "Lloyd Benson",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/fishin/mock/issues"
  },
  "homepage": "https://github.com/fishin/mock",
  "engines": {
    "node": ">=0.12.x"
  },
  "scripts": {
    "clean": "rm -rf node_modules",
    "console": "lab -a code -t 100 -v -m 2000 -L",
    "test": "lab -a code -t 100 -v -m 2000 -L -r json -o lab.json -r html -o lab.html -r console -o stdout -r console -o console.log"
  },
  "dependencies": {
    "hapi": "8.x.x"
  },
  "devDependencies": {
    "code": "1.x.x",
    "lab": "5.x.x",
    "wreck": "5.x.x"
  }
}
